<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 🔒 PRODUCTION: Only HTTPS allowed by default -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- 🛠️ DEVELOPMENT: Allow localhost HTTP for testing -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain> <!-- Android emulator host -->
    </domain-config>

    <!-- ⚠️ WARNING: Never add production IPs/domains here! -->
    <!-- Production must ALWAYS use HTTPS/WSS -->
</network-security-config>
