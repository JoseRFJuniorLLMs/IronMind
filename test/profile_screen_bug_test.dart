import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Profile screen bug test (disabled)',
      (WidgetTester tester) async {
    // Teste desativado temporariamente
    expect(true, true);
  });
}
